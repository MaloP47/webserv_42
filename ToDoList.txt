- Is each mandatory field present in server block : 
	- port
	- root
	- index
	- ( host ) : 127.0.0.1
	- ( name ) : "Server" + (_nbServer + 1)
	- ( default error page ) : error/404.html
	- ( max body size ) : 999?
	- ( location ) 
		- ( methods GET POST ) : GET
		- ( redirect bool & redirect path ) : false 
		- ( autoindex ) : false
		- ( location root ) 
		- ( index for route )
	- ( upload path )
	- ( cgi Only get et post )
		- supported extensions
		- /usr/bin/path

- List de tous les ports occupe par la creation, servir le premier par default ( desactive les suivants, si il y a deja 8080, les autres ne sont pas cree)
